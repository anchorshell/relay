# Core characterization bundle

Approved `actions.asft` and `metadata.asft` artifacts are copied here by the
private release process. The hand-authored dev model is never published as the
core model.
